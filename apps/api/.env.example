# API-specific overrides for local development.
# Values fall back to the repo-root .env if not set here.

# Uncomment to override Postgres host when running the API on the host
# and Postgres inside Docker (both use localhost by default):
# POSTGRES_HOST=localhost

# LangChain / Anthropic
# ANTHROPIC_API_KEY=
